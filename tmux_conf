# change tmux leader to ctrl s
unbind C-b
set -g prefix C-s

set -g default-terminal "screen-256color"

# extra space on left side status blob
set -g status-left-length 50

# use vim keybindings in copy/scroll mode
setw -g mode-keys vi
